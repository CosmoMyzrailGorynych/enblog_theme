<nav class="aPagination">
    {{#unless first}}
        <a href="/enblog_demo{{prev_page}}" class="aPagination-aPrevious">{{previous_page_label}}</a>
    {{/unless}}
    {{#unless last}}
        <a href="/enblog_demo{{next_page}}" class="aPagination-aNext">{{next_page_label}}</a>
    {{/unless}}
    <div style="clear: both;"></div>
</nav>
